import SwiftUI

struct ContentView: View {
    var body: some View {
        VStack {
            Text("Kids Eat Free")
                .font(.largeTitle)
                .fontWeight(.black)
        }
    }
}
